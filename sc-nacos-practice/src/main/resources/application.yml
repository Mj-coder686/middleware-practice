server:
  port: 8086

spring:
  application:
    name: nacos-practice

  cloud:
    nacos:
      # 服务注册发现
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP
        # 权重、元数据等你来配

  # 配置中心 (bootstrap.yml 优先, 这里留作说明)
  # cloud:
  #   nacos:
  #     config:
  #       server-addr: localhost:8848
  #       file-extension: yaml
  #       group: DEFAULT_GROUP
  #       namespace: public

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
