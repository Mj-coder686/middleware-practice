server:
  port: 8085

xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin  # XXL-JOB 调度中心地址
    accessToken:
    executor:
      appname: xxl-job-practice-executor
      ip:
      port: 9999  # 执行器端口, 0 表示自动分配
      logpath: ./logs/xxl-job/jobhandler
      logretentiondays: 30

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
